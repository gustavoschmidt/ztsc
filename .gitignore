.zig-cache/
zig-out/
bench/corpus/
node_modules/

# real-application benchmark checkouts (multi-GB, cloned + installed locally;
# see BENCHMARKS.md) — like bench/corpus, regenerated rather than vendored
bench/apps/

# Microsoft's TypeScript test corpus (Apache-2.0), fetched by
# bench/fetch_ts_suite.sh for the ztsc-vs-tsgo divergence sweep, plus that
# sweep's materialized cases and reports. Deliberately never vendored — see
# the licensing note at the top of bench/fetch_ts_suite.sh.
bench/ts-suite/

# Claude Code local state — agent worktrees are full checkouts plus build cache
.claude/

# local repros, diff dumps and throwaway probes
scratchpad/

# but keep the node_modules fixtures used by conformance tests
!test/conformance/**/node_modules/
!test/conformance/**/node_modules/**

# Python bytecode from the bench scripts.
__pycache__/
bench/baselines/tsgo/node_modules
