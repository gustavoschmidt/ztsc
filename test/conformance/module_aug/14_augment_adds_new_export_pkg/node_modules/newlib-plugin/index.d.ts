import { Base } from "newlib";
declare module "newlib" {
  interface Base {
    fromPlugin: number;
  }
  // `Extra` is introduced by a *sibling* augmentation block in another file:
  // an unqualified name inside an augmentation sees the augmented module's
  // exports, which now include what other blocks added.
  interface Wrap {
    inner: Extra;
  }
}
export {};
