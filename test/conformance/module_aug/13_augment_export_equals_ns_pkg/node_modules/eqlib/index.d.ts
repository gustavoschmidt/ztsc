/// <reference path="./extra.d.ts" />
export = _;
declare const _: _.Api;
declare namespace _ {
  // Deliberately empty: every member arrives by augmentation, the
  // DefinitelyTyped `@types/lodash` shape.
  interface Api {}
}
