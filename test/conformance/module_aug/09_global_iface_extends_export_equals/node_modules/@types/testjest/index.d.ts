export {};
declare global {
  namespace jest {
    interface Matchers<R, T = {}> {
      toBe(expected: T): R;
    }
    interface Expect {
      <T = unknown>(actual: T): Matchers<void, T>;
    }
  }
}
