// A file INSIDE the package augmenting the package's own name. Its only
// import is relative, so "mylib" appears nowhere but in the augmentation
// header — tsc resolves it anyway (`getModuleNames` appends
// `file.moduleAugmentations` to `file.imports`).
import type { Tag } from "./types.js";
declare module "mylib" {
  interface Base {
    extra: number;
  }
}
export declare const marker: Tag;
