import "mylib";
// A cross-package interface augmentation of another package's CLASS: the
// `interface Widget` block merges its members into `class Widget`'s instance.
declare module "mylib" {
  interface Widget {
    extra: number;
  }
}
