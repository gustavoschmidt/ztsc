# Reviewed divergences between the pinned oracle and ztsc.
#
# `.expected` snapshots are pure oracle output and are never hand-edited, so
# `node test/conformance/gen_expected.js test/conformance --check` is a real
# gate. Every deliberate difference is recorded here instead, one per line:
#
#   <case-path>  -TS<code> [<file>] <line>   oracle reports it, ztsc does not
#   <case-path>  +TS<code> [<file>] <line>   ztsc reports it, the oracle does not
#
# (`<file>` only for directory cases, matching the snapshot format.)
#
# Policy: a missed error (`-`) is acceptable when it is deterministic and
# reasoned; a false positive is not. A `+` entry therefore needs a stronger
# justification than a `-` entry and is only defensible when the same
# diagnostic is also present in the snapshot at a different span.
#
# The registry is self-cleaning: run_conformance.zig fails the case if an entry
# stops describing reality (ztsc started emitting a `-` line, stopped emitting
# a `+` line, the oracle dropped a `-` line, or the case is gone). It cannot be
# used to hide a regression.

# TS2636 — the DECLARATION-site half of TS 4.7 variance: that an `in`/`out`
# annotation matches how the parameter is actually used. tsc measures a
# parameter's structural variance by instantiating the generic against opaque
# sub/super marker types and relating the two instantiations; ztsc has no
# marker type parameters, so the measurement (not just the report) is missing.
# The USE-site half — relating two instantiations by their annotated arguments
# — is implemented, and this case pins exactly that: the annotation on line 7
# is the one tsc flags, and the assignment on line 13 goes by the annotation
# rather than by the member. A deterministic under-report of a whole check;
# assignability/075 is the positive/negative pair it under-reports against.
assignability/077_variance_annotation_mismatch.ts  -TS2636 7

# TS2417 (class static side incorrectly extends base static side) is not
# implemented: ztsc does not relate `typeof Derived` to `typeof Base` when a
# derived static shadows a base static incompatibly. Under-report of a whole
# check, not a span/precision difference.
classes/033_static_inherit.ts  -TS2417 14

# TS2589 at the structural-relation recursion cap. ztsc's cap assumes the
# relation and stays silent rather than reporting at the cap, because reporting
# there would false-positive genuinely-valid deep recursive types the oracle
# accepts (real form-library `PathValue` shapes). Deliberate, deterministic
# under-report; the cases exist to prove these inputs keep checking without
# stack-overflowing. See the comment in each case file.
instantiation/003_recursive_alias_relation_cap.ts  -TS2589 19
instantiation/010_growing_bareref_no_eager_expand.ts  -TS2589 12

# A growing recursive alias, cut by the instantiation chain-repetition guard
# (`max_chain_repeats`) rather than by the depth ceiling. Same handling as 003
# and 010: the cut is silent, so the oracle's TS2589 is a deterministic
# under-report. The TS2322 on line 18 still matches.
instantiation/018_growing_template_hole_no_loop.ts  -TS2589 16

# An `exports`-map subpath that the map does not name is routed to an opaque
# `any` module rather than left dangling. Dangling the specifier was not
# crash-safe under parallel resolution (it dangled a symbol -> SIGABRT), so the
# TS2307 the oracle emits is deliberately traded for crash safety. See the
# comment in the case entry.ts.
modules/39_exports_subpath_block  -TS2307 entry.ts 12

# TS2578 ("Unused '@ts-expect-error' directive") is not implemented. The
# suppression half of `@ts-expect-error` is; the unused report is not, because
# it is only sound on a checker that reports everything the oracle does. ztsc
# deliberately under-reports in places, so a directive the oracle considers used
# can look unused here, and TS2578 on that basis would be a false positive on
# correct code — the one thing project policy forbids. Deterministic
# under-report of a whole check.
directives/012_ts_expect_error_unused.ts  -TS2578 7

# A compound assignment (`x += 1`, `x -= 1`, ...) does not check that its
# result is assignable back to the target, so the oracle's TS2322 on
# `mv += 1` where `mv: number & { _brand }` is missed. Pre-existing and
# independent of the operand classification the case is about: the plain
# assignments on lines 11-12 of the same case prove `number` is still not
# assignable to the brand. Deterministic under-report of a whole check.
operators/004_branded_intersection_not_numeric.ts  -TS2322 31

# The callback-parameter relation is ADDITIVE in ztsc, where tsc makes it the
# only test for a pair of single-call-signature parameters. When the callback
# comparison fails, ztsc falls back to the pre-existing
# contravariant/bivariant parameter check instead of rejecting, so the two
# directions tsc rejects *because* the callback relation is exclusive are
# under-reported: a method callback related in the contravariant direction
# (line 25), and a callback nested one level deeper, which tsc relates
# strictly (line 74). Made exclusive, the rule also rejects pairs
# whole-signature bivariance accepted, and every such case measured across the
# 16-package corpus was an unrelated inference gap surfacing
# (`Observable<unknown>` where tsc infers `Observable<number>`) — i.e. it would
# trade this fix for a batch of false positives, which policy forbids. The 7
# positives in the same case prove the relation itself is in place.
assignability/065_callback_parameter_variance.ts  -TS2322 25
assignability/065_callback_parameter_variance.ts  -TS2322 74

# TS2502 ("referenced directly or indirectly in its own type annotation") is
# not implemented. A member whose annotation indexes at itself is genuinely
# circular; `lazyRefProp` detects the re-entry and cuts it, but the cut is
# silent — it falls back to the ordinary property-not-found result rather than
# reporting. Deterministic under-report of a whole check. The case exists to
# pin the CUT: without it the lazy single-member lookup would recur forever,
# and the non-circular self-index in the same file (line 23) proves the
# lookup itself still resolves.
indexed/021_self_referential_member_annotation.ts  -TS2502 7
indexed/021_self_referential_member_annotation.ts  -TS2502 11
indexed/021_self_referential_member_annotation.ts  -TS2502 12

# The three circularity reports for a class member reached from its own body
# — TS7023 (inferred return type), TS7022 (inferred field type), TS2729 (field
# read before its initialization) — are not implemented. The same silent cut as
# TS2502 above: `lazyRefProp` detects the re-entry and falls back to the
# ordinary not-found path rather than naming the cycle. Deterministic
# under-reports of whole checks; the case exists to pin the cut (without it the
# lookup recurs forever), and the non-circular member on line 57 proves the
# lookup still resolves.
classes/062_inferred_return_this_cycle.ts  -TS7023 18
classes/062_inferred_return_this_cycle.ts  -TS7023 25
classes/062_inferred_return_this_cycle.ts  -TS7023 28
classes/062_inferred_return_this_cycle.ts  -TS7022 35
classes/062_inferred_return_this_cycle.ts  -TS2729 35
classes/062_inferred_return_this_cycle.ts  -TS2729 41

# A rest parameter typed by a UNION is modelled per POSITION at a CALL: each
# argument position gets the union of the constituents' element types (tsc's
# `getIndexedAccessType(restType, number)`), widened by `undefined` where some
# constituent makes the position optional. That is what contextually types a
# callback argument and what accepts an optional options bag. tsc additionally
# relates the argument list *as a whole* to the rest type
# (`getSignatureApplicabilityError` does zero positional checks once
# `getNonArrayRestType` answers), so it rejects a list that mixes constituents:
# a bare handler and an array of handlers in one call (041), an options bag in
# the defaultValue slot (053). ztsc accepts those — a deterministic under-report
# of one extra check.
#
# Its SIGNATURE-RELATION half is implemented (`sigRestUnion` /
# `restTupleAtPosition`, conformance calls/053), where whole-list arm choice is
# what an assignment actually turns on. The call half additionally needs `const`
# type parameters (tsc's `isConstTypeVariable` puts a template-literal argument
# written for a `const Key extends …` rest arm in a const context, so it keeps
# its literal type); without them the whole-list check rejects `t(`P.${x}`)` at
# every i18next call site, i.e. it would trade this under-report for a batch of
# false positives, which policy forbids.
#
# 041: the per-position check still discriminates — an incompatible callback is
# rejected on line 31.
calls/041_rest_param_union_of_arrays.ts  -TS2345 27
calls/053_rest_param_union_of_tuples.ts  -TS2345 58

# ztsc emits no TS2536 at all — the "key K cannot be used to index T" check on
# a deferred indexed access is unimplemented. The assignment through the
# unusable access is still rejected (the TS2322 on the same line), so nothing
# passes silently; only the second, more specific diagnostic is missing.
indexed/028_index_target_constraint_negatives.ts  -TS2536 31

# Element access with a UNION key is distributed only when every constituent
# resolves (indexed/029); when one does not, the access keeps the `any` it
# always had. tsc reports TS7053 there — an implicit-'any' element access — and
# ztsc's element-access path emits TS7053 only for a SINGLE unresolved key, not
# for a key set. Line 19 is the same shape one level down: the union reaches the
# tuple's numeric index signature, so the assignment is still rejected (the
# TS2322 on that line), but the out-of-range constituent is not named.
# Deterministic under-reports; the case exists to pin that the distribution
# stays out of these three.
indexed/030_element_access_union_key_negatives.ts  -TS7053 9
indexed/030_element_access_union_key_negatives.ts  -TS7053 14
indexed/030_element_access_union_key_negatives.ts  -TS2493 19

# A cast whose source or target is a DEFERRED indexed access (`T[K]`) is
# conceded outright (indexed/031): tsc rejects one only when
# `getBaseConstraintOfType` fully reduces the access, which needs the object
# constraint to answer for every key the index constraint admits. Reducing it
# structurally instead — substituting each parameter's constraint — also
# collapses the shapes tsc leaves deferred, and then rejects casts tsc accepts
# (`s as T[K]` under `T extends { a: number; b: number }`, plus four more in
# 031). Trading one deterministic under-report for five false rejections is the
# standing policy direction. Line 14, whose operands are not deferred, still
# reports.
indexed/032_cast_to_deferred_indexed_access_negatives.ts  -TS2352 8

# An explicit `this` parameter is not part of ztsc's signature relation, so a
# function expression whose own `this` contradicts its contextual type is
# accepted where tsc reports TS2322 on the declaration. The body is still
# checked against the declared `this` — line 52, which proves the own parameter
# wins over the contextual one — so only the outer compatibility report is
# missing. Deterministic under-report of one check; pre-dates the contextual
# `this` work and is independent of it.
inference/094_contextual_this_parameter.ts  -TS2322 51

# Excess-property checking does not descend an INTERSECTION contextual type:
# `excessPropertyCheck` walks the target's own property list, and an
# intersection has none of its own, so an unknown property in an object literal
# passed to `f(o: {…} & Opts)` is not reported. Pre-existing and independent of
# the contextual-type fix the case covers (base binary is identically silent);
# a deterministic under-report of one check. The wrong-branch TS2322 rows on
# lines 12/14/16/20 — what the case is actually about — all match.
inference/100_literal_ctx_through_intersection_param_negatives.ts  -TS2353 25

# Reading a property off an intersection whose members include a still-GENERIC
# mapped type answers from that mapped type instead of reporting TS2339: ztsc's
# `propOfType` synthesizes a member for any name on `Partial<Record<T, any>>`,
# where tsc's `getPropertyOfType` has no members at all until `T` is known. The
# `in`-narrowing side of the same question IS handled (`propDeclaredForIn`, the
# subject of case 060); the READ side is a wider change to property lookup.
# Pre-existing and deterministic: the base binary is identically silent here,
# and line 13 — the same read on the other branch — still reports.
narrowing/061_in_narrow_generic_mapped_constituent_negatives.ts  -TS2339 21

# ztsc's conditional-TARGET rule ("the source must satisfy whichever branch the
# conditional resolves to, so require it against both") is a general
# assignability rule. tsc applies the same rule only in a RETURN position — the
# 5.8 "checked returns for conditional types" feature — and rejects an identical
# assignment in a variable declaration. Case 035 pins the exact extent of the
# leniency: the four `decl*` rows below are accepted by ztsc and rejected by
# tsc, while every RETURN row and the miss-a-branch row (41) agree.
# Pre-existing for object, union and primitive sources (the base binary is
# identically silent on 22/26/34); line 30 joined them when the intersection
# source stopped being answered `false` before it could reach the rule, which is
# what made a UNION OF INTERSECTIONS unable to meet a conditional spelled with
# that very union. A deterministic under-report on all four.
conditional/035_deferred_conditional_target_leniency.ts  -TS2322 22
conditional/035_deferred_conditional_target_leniency.ts  -TS2322 26
conditional/035_deferred_conditional_target_leniency.ts  -TS2322 30
conditional/035_deferred_conditional_target_leniency.ts  -TS2322 34

# Same root as the 035 block: an intersection source now reaches the general
# conditional-target rule, and `Radians` satisfies both branches of
# `T extends 0 ? Radians : Radians`. tsc rejects it here (declaration position)
# and accepts it in a return position; this case only exercises the declaration
# half. The `T extends 0 ? string : string` row on line 28 — the case's actual
# negative — still reports.
assignability/066_intersection_source_deferred_target.ts  -TS2322 27

# A property read on `never` is not reported. ztsc's member-access path treats
# `never` as permissive (it is assignable to everything, so a read off it can
# never widen a later error), and reporting it would need a dedicated arm.
# Pre-existing and unrelated to what case 085 pins: the base binary is
# identically silent on a bare `(x: never) => x.length`. Line 41 is the branch
# `typeof x === "string"` leaves empty when `T`'s constraint has no string
# constituent — the collapse itself is what the case asserts, and every other
# row in the file reports.
narrowing/085_typeof_keeps_type_param_negatives.ts  -TS2339 41
