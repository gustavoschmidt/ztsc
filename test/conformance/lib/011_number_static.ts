const a: boolean = Number.isInteger(5);
const b: boolean = Number.isFinite(5.0);
const c: boolean = Number.isNaN(NaN);
const d: number = Number.parseInt("42", 10);
const e: number = Number.parseFloat("3.14");
const f: number = Number.MAX_SAFE_INTEGER;
const g: number = Number.MIN_SAFE_INTEGER;
const h: number = Number.EPSILON;
