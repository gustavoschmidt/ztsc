const nums: number[] = [1, 2, 3];
const bad: string = nums.map((x) => x * 2);
nums.push("four");
const s: string = "hi";
const wrong: number = s.toUpperCase();
Math.floor("nope");
const missing = nums.nonexistent();
