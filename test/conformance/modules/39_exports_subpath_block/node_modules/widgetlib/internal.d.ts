// On disk at the legacy-probeable path (node_modules/widgetlib/internal.d.ts):
// without the exports-subpath block, ztsc would resolve `widgetlib/internal`
// here and type `secret` as `string`. The present `exports` map (which omits
// this subpath) must block that, degrading `secret` to `any`.
export declare const secret: string;
