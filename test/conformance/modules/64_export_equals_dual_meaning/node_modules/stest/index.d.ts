import { Request } from "sagent";

// `Request` has to be usable as a VALUE here (the const's `Request` property,
// `typeof SARequest`), so `Test` inherits every SARequest member.
declare class Test extends Request {
  expect(status: number): this;
}

export = Test;
