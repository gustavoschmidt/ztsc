// The TOP-level copy (v20): "sockbuf" is the primary declaration and
// "node:sockbuf" the star alias — exactly how `@types/node@20`'s buffer.d.ts is
// written. The `global` block inside the ambient module is where the shared
// interface lives; the module only re-exports the global name.
declare module "sockbuf" {
    global {
        interface SockBuf {
            fromTop: string;
        }
    }
    export { SockBuf };
}
declare module "node:sockbuf" {
    export * from "sockbuf";
}
