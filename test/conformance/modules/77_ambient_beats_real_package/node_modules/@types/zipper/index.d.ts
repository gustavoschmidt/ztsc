// `@types/yazl`'s shape: a `types` reference that picks up its OWN nested copy
// of the shared types package, then a bare `import … from "sockbuf"` — resolved
// from inside node_modules, where the real `sockbuf` package is a sibling.
/// <reference types="fakenode" />

import { SockBuf } from "sockbuf";

export declare function addBuffer(b: SockBuf): void;
export declare function roundTrip(b: SockBuf): SockBuf;
