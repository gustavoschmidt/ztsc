// The NESTED copy (v25). The newer release flipped the pair around: the primary
// declaration is now "node:sockbuf" and "sockbuf" is the star alias — so the two
// copies form a star-export CYCLE once merged by name, and the interface each
// contributes must still land on one global symbol.
declare module "node:sockbuf" {
    global {
        interface SockBuf {
            fromNested: number;
        }
    }
    export { SockBuf };
}
declare module "sockbuf" {
    export * from "node:sockbuf";
}
