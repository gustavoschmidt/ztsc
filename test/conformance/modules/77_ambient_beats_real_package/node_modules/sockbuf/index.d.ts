// A real, resolvable package named exactly like the ambient module — `buffer`
// (feross' Node Buffer API for the browser) is the one in the wild. Its
// `SockBuf` is unrelated to the global one, and nothing in the program may bind
// to it: every name below is deliberately absent from the ambient interface, so
// picking this file over the declaration cannot be silent.
export declare class SockBuf {
    private polyfillBrand;
    fromPolyfill: boolean;
}
