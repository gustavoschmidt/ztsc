// A file-level star of a FILE whose own table only gains the ambient names
// later: the two halves of the star merge have to reach a joint fixed point,
// not each settle alone.
export * from "starpkg2";
