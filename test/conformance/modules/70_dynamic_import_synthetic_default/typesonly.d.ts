// A declaration file that exports only TYPES: its namespace object has no
// value members at all, and it declares no `default`.
export interface Data {
  a: string;
}
export interface Other {
  b: number;
}
