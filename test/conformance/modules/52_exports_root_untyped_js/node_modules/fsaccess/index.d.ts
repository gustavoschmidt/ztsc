// Reachable only through the top-level `"types"` field, which the `exports`
// map hides: the map's `.` entry names `./dist/index.mjs`, and there is no
// `./dist/index.d.mts` beside it. tsc stops at the exports result and treats
// the package as untyped JavaScript; resolving THIS file instead would type
// `fileOpen`'s callback parameters and silence every diagnostic in the case.
export declare function fileOpen(opts: {
  legacySetup?: (resolve: (v: string) => void, input: number) => void;
}): Promise<string>;
