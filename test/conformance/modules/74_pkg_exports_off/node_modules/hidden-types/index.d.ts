// Reachable only through the legacy `"types"` field (the `exports` map names no
// `types` condition), so it resolves ONLY with resolvePackageJsonExports off.
export declare const thing: { n: number };
