// A subpath the `exports` map does not name. With the map honored the specifier
// is blocked (a closed set of entry points → TS2307); with the option off there
// is no map, so the plain relative stem under the package resolves.
export declare function tag(): string;
