// The `@types/lodash.debounce` shape: a single-method package that is nothing
// but a re-export of one property of another package's `export =` value.
import { twice } from "valpkg";
export = twice;
