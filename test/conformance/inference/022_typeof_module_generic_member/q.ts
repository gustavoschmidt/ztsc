export declare function getByText<T extends object = object>(
  container: object,
  id: string,
): T
export declare function findByRole<T extends object = object>(
  container: object,
  id: string,
): T
